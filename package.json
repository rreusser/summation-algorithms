{
  "name": "summation-algorithms",
  "version": "1.0.0",
  "description": "Naive, pairwise recursive, pairwise non-recursive, and Kahan summation algorithms, compared",
  "main": "numeric-sum-benchmark.js",
  "scripts": {
    "test": "semistandard && mocha",
    "benchmark": "node --allow-natives-syntax benchmark/index.js",
    "trace": "node --trace-opt --trace-deopt --allow-natives-syntax --code-comments trace.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "almost-equal": "^1.1.0",
    "benchmark": "^2.1.0",
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "ndarray": "^1.0.18",
    "optimized": "^1.2.0",
    "semistandard": "^7.0.5"
  },
  "semistandard": {
    "globals": [
      "describe",
      "it",
      "beforeEach"
    ]
  }
}
